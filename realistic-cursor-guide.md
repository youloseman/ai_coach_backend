# 🎯 Практичный подход: Cursor AI Prompt для Week 1

## ⚠️ ВАЖНО: Как работать с Cursor AI

**НЕ ЗАПУСКАЙ ВСЁ СРАЗУ В AUTO MODE!**

Cursor AI - это помощник, но не полностью автономный разработчик. Лучший результат будет при **пошаговом подходе**.

---

## 📝 Стратегия работы

### Вариант A: Постепенный (РЕКОМЕНДУЮ)
✅ Даешь по 1 задаче за раз
✅ Проверяешь результат
✅ Тестируешь в браузере
✅ Переходишь к следующей

### Вариант B: Полуавтоматический (можно попробовать)
⚠️ Даешь несколько связанных задач
⚠️ Cursor делает, ты проверяешь
⚠️ Фиксишь ошибки

### Вариант C: Полный автомат (НЕ СОВЕТУЮ)
❌ Высокий риск багов
❌ Может сломать существующий код
❌ Потратишь больше времени на исправления

---

## 🚀 ДЕНЬ 1: ЗАДАЧА 1 - API Functions (30 минут)

### Промпт для Cursor Composer:

```
Я работаю над AI Triathlon Coach на Next.js 16 + TypeScript.

КОНТЕКСТ:
- У меня уже есть файл frontend/lib/api.ts с функциями authAPI, profileAPI, coachAPI
- Backend (FastAPI) имеет готовые эндпоинты в main.py: /analytics/training_load, /analytics/form_status, /analytics/fatigue, /analytics/predict_race, /analytics/all_predictions
- Мне нужно добавить функции для вызова этих эндпоинтов

ЗАДАЧА:
Добавь в frontend/lib/api.ts новый объект analyticsAPI с 5 функциями:

1. getTrainingLoad(weeks: number = 12)
   - GET /analytics/training_load?weeks={weeks}
   
2. getFormStatus()
   - GET /analytics/form_status
   
3. getFatigueAnalysis(weeks: number = 4)
   - GET /analytics/fatigue?weeks={weeks}
   
4. predictRace(params: {goalRaceType, goalTime, sport?, weeks?})
   - GET /analytics/predict_race с query params
   
5. getAllPredictions(sport = 'run', weeks = 12)
   - GET /analytics/all_predictions?sport={sport}&weeks={weeks}

ТРЕБОВАНИЯ:
- Используй существующий axios instance (api)
- Добавь try-catch с console.error
- Добавь JSDoc комментарии
- Следуй стилю существующих функций в файле
- НЕ изменяй существующие функции

Также добавь в stravaAPI функцию:
- getActivities(page = 1, perPage = 10)
  - GET /strava/activities?page={page}&per_page={perPage}

Покажи мне только изменения (diff), не весь файл.
```

**ЧТО ПРОВЕРИТЬ ПОСЛЕ:**
```bash
cd frontend
npx tsc --noEmit  # Проверка TypeScript
npm run dev       # Запуск dev server
```

Открой браузер, проверь консоль - не должно быть ошибок импорта.

---

## 🚀 ДЕНЬ 1: ЗАДАЧА 2 - TypeScript Types (15 минут)

### Промпт для Cursor:

```
Теперь добавь TypeScript типы для ответов от этих API.

ФАЙЛ: frontend/types/index.ts

Добавь следующие интерфейсы:

1. TrainingLoadAnalysis - для /analytics/training_load
   Поля: status, analysis { current_ctl, current_atl, current_tsb, current_ramp_rate, weekly_tss: array }

2. FormStatus - для /analytics/form_status
   Поля: status, current_date, current_ctl, current_atl, current_tsb, form { label, color, description, recommendation }

3. FatigueAnalysis - для /analytics/fatigue
   Поля: status, overall_fatigue_level, fatigue_score, signals: array, recommendations: array, metrics: object

4. RacePrediction - для /analytics/predict_race
   Поля: status, prediction { goal_race_type, predicted_time, probability_of_success, recommendations: array }

5. StravaActivity - для /strava/activities
   Поля: id, name, sport_type, start_date, distance_meters, moving_time_seconds, average_heartrate?, tss?

ВАЖНО:
- Все поля должны быть правильно типизированы (string | number | boolean | array | object)
- Используй optional (?) для необязательных полей
- Добавь экспорты: export interface ...

Покажи только новые интерфейсы.
```

**ЧТО ПРОВЕРИТЬ:**
```bash
npx tsc --noEmit  # Должно быть 0 ошибок
```

---

## 🚀 ДЕНЬ 1: ЗАДАЧА 3 - Form Status Card (45 минут)

### Промпт для Cursor:

```
Создай новый компонент FormStatusCard.

ФАЙЛ: frontend/components/FormStatusCard.tsx

ТРЕБОВАНИЯ:
1. 'use client' в начале
2. Используй analyticsAPI.getFormStatus() из @/lib/api
3. Используй тип FormStatus из @/types
4. Состояния: loading, error, data
5. useEffect для загрузки данных при монтировании

LAYOUT:
┌─────────────────────────────┐
│ Current Form                │
│                             │
│  [BADGE: Fresh/Optimal/     │
│          Fatigued]          │
│                             │
│  Fitness: 95  Fatigue: 65   │
│  Form: +30                  │
│                             │
│  Description text...        │
│                             │
│  💡 Recommendation: ...     │
└─────────────────────────────┘

ДИЗАЙН:
- bg-white rounded-lg shadow p-6
- Badge с цветами: green/yellow/orange/red (зависит от form.color)
- Метрики в grid grid-cols-3
- Синяя рамка для рекомендации (bg-blue-50 border-l-4 border-blue-400)

LOADING STATE:
- Показывай скелетон (animate-pulse с серыми блоками)

ERROR STATE:
- Показывай сообщение: "Connect Strava and sync activities"

ВАЖНО:
- Не используй inline styles, только Tailwind классы
- Добавь типы для всех useState
- Обрабатывай случай когда formStatus = null

Создай полный компонент.
```

**ЧТО ПРОВЕРИТЬ:**
1. Компонент создан в правильной папке
2. Компилируется без ошибок TypeScript
3. Импорты работают

Для теста добавь временно в `app/dashboard/page.tsx`:
```typescript
import FormStatusCard from '@/components/FormStatusCard';

// В JSX где-нибудь:
<FormStatusCard />
```

Открой http://localhost:3000/dashboard и проверь:
- Компонент рендерится
- Показывает loading
- Делает API запрос (смотри Network tab)
- Показывает данные ИЛИ ошибку

---

## 🚀 ДЕНЬ 1-2: ЗАДАЧА 4 - Остальные компоненты

**ПРИМЕНЯЙ ТУ ЖЕ СТРАТЕГИЮ:**

### Для каждого компонента:

```
Создай компонент [НАЗВАНИЕ].

[Описание что делает]
[Layout схема]
[Требования к дизайну]
[API которое использует]

Создай полный компонент.
```

**Компоненты по порядку:**
1. ✅ FormStatusCard (сделали)
2. FatigueWarningBanner - предупреждение (только при high fatigue)
3. RacePredictionCard - прогноз времени
4. ActivityCard - одна карточка тренировки
5. RecentActivitiesList - список карточек

**После каждого:**
- Проверь TypeScript
- Добавь временно на Dashboard
- Проверь в браузере
- Только потом следующий!

---

## 🚀 ДЕНЬ 2: ЗАДАЧА 5 - Интеграция в Dashboard (1 час)

### Промпт для Cursor:

```
КОНТЕКСТ: У меня готовы компоненты:
- FormStatusCard
- FatigueWarningBanner  
- RacePredictionCard
- RecentActivitiesList

ЗАДАЧА: Интегрируй их в app/dashboard/page.tsx

ГДЕ ДОБАВИТЬ:
1. Найди секцию с Performance Chart (PerformanceChart component)
2. ПОСЛЕ неё добавь:

```tsx
{/* Analytics Cards */}
<div className="grid md:grid-cols-2 lg:grid-cols-3 gap-6 mb-8">
  <FormStatusCard />
  <RacePredictionCard />
</div>

{/* Fatigue Warning */}
<FatigueWarningBanner />

{/* Recent Activities */}
<div className="bg-white rounded-lg shadow p-6 mb-8">
  <h2 className="text-xl font-bold mb-4">Recent Activities</h2>
  <RecentActivitiesList />
</div>
```

ТРЕБОВАНИЯ:
- Добавь импорты вверху файла
- НЕ удаляй существующий код
- НЕ меняй существующие компоненты
- Просто вставь новый код в правильное место

Покажи мне diff изменений.
```

**ЧТО ПРОВЕРИТЬ:**
```bash
npm run dev
```

Открой Dashboard:
- Все компоненты видны
- Правильный порядок
- Нет ошибок в консоли
- Загружаются данные

---

## 🚀 ДЕНЬ 3: ЗАДАЧА 6 - Dark Mode (2 часа)

### Шаг 6.1: Установка (Cursor не может, делай сам!)

```bash
cd frontend
npm install next-themes
```

### Шаг 6.2: Промпт для настройки

```
Настрой dark mode для проекта.

ЗАДАЧИ:

1. Обнови app/providers.tsx (или создай если нет):
```tsx
'use client';
import { ThemeProvider } from 'next-themes';

export function Providers({ children }: { children: React.ReactNode }) {
  return (
    <ThemeProvider attribute="class" defaultTheme="light" enableSystem>
      {children}
    </ThemeProvider>
  );
}
```

2. Оберни app в provider в app/layout.tsx:
   - Импортируй Providers
   - Оберни children: <Providers>{children}</Providers>

3. Создай components/ThemeToggle.tsx:
   - Кнопка переключения темы (sun/moon иконки)
   - Используй useTheme() из next-themes
   - Добавь mounted check (чтобы не было hydration mismatch)

4. Обнови tailwind.config.js:
   - Добавь: darkMode: 'class'

Сделай по порядку, покажи код каждого шага.
```

### Шаг 6.3: Добавление dark mode классов

**НЕ ДАВАЙ CURSOR СДЕЛАТЬ ВСЁ СРАЗУ!**

Лучше так:

```
Добавь dark mode стили ТОЛЬКО в FormStatusCard компонент.

ПАТТЕРН:
bg-white → bg-white dark:bg-gray-800
text-gray-900 → text-gray-900 dark:text-gray-100
text-gray-500 → text-gray-500 dark:text-gray-400
border → border dark:border-gray-700
shadow → shadow dark:shadow-gray-700

Примени этот паттерн ко всем элементам в FormStatusCard.
Покажи diff.
```

Потом по одному для каждого компонента:
- FatigueWarningBanner
- RacePredictionCard
- ActivityCard
- RecentActivitiesList
- Dashboard page

---

## 🚀 ДЕНЬ 4: Activity Maps (ОПЦИОНАЛЬНО)

Это сложнее, можно пропустить или сделать позже.

```bash
npm install leaflet react-leaflet @types/leaflet
```

Скажи Cursor:
```
Создай компонент ActivityMap для отображения маршрута тренировки.

ИСПОЛЬЗУЙ: react-leaflet
API: принимает polyline (string)
LAYOUT: MapContainer с TileLayer и Polyline

Если polyline нет - показывай placeholder "No map data"

Создай компонент.
```

---

## ✅ ЧЕКЛИСТ ПРОВЕРКИ ПОСЛЕ КАЖДОЙ ЗАДАЧИ

После каждого промпта для Cursor:

```bash
# 1. TypeScript компилируется?
npx tsc --noEmit

# 2. Dev server работает?
npm run dev

# 3. В браузере:
- Открыл страницу?
- Компонент отображается?
- Нет ошибок в Console?
- Нет ошибок в Network tab?
- Данные загружаются?

# 4. Всё ОК? → Git commit
git add .
git commit -m "Add FormStatusCard component"
```

**ТОЛЬКО ПОСЛЕ ЭТОГО** переходи к следующей задаче!

---

## 🐛 Когда Cursor делает ошибки (будет 100%)

### Проблема 1: TypeScript ошибки
```
Cursor сделал, но TypeScript ругается.
```

**Решение:**
```
Cursor AI, исправь TypeScript ошибки:

[скопируй ошибку из терминала]

Исправь типы и покажи diff.
```

### Проблема 2: Компонент не рендерится
```
Компонент создан, но ничего не показывается.
```

**Решение:**
1. Проверь импорты
2. Проверь экспорт (export default)
3. Открой React DevTools
4. Проверь Console на ошибки

Скажи Cursor:
```
Компонент FormStatusCard не отображается. 
Проверь:
1. Правильный ли export default
2. Все ли импорты на месте
3. Нет ли ошибок в JSX

Исправь проблему.
```

### Проблема 3: API не работает
```
API запрос уходит, но данные не приходят или ошибка.
```

**Решение:**
1. Открой Network tab в Chrome
2. Найди запрос
3. Проверь Response

Скажи Cursor:
```
API запрос к /analytics/form_status возвращает ошибку 500.

Проверь:
1. Правильный ли URL (должен быть relative /analytics/...)
2. Правильные ли headers
3. Обработка ошибок в try-catch

Backend endpoint точно работает, проблема в frontend коде.
Исправь.
```

### Проблема 4: Стили сломались
```
Cursor добавил dark mode классы, но теперь light mode сломался.
```

**Решение:**
```
Ты добавил dark mode классы, но сломал light mode.

Правило: ВСЕГДА сначала light, потом dark:
✅ Правильно: bg-white dark:bg-gray-800
❌ Неправильно: dark:bg-gray-800 bg-white

Исправь все классы в FormStatusCard.
Покажи diff.
```

---

## 🎯 РЕАЛИСТИЧНЫЕ ОЖИДАНИЯ

### Что Cursor МОЖЕТ:
- ✅ Создать 80% кода правильно
- ✅ Следовать структуре проекта
- ✅ Применить паттерны из существующего кода
- ✅ Создать базовую функциональность

### Что Cursor НЕ МОЖЕТ:
- ❌ Понять контекст на 100%
- ❌ Протестировать код в браузере
- ❌ Исправить все edge cases
- ❌ Знать как выглядит твой UI

### Твоя роль:
- 🧠 Проверять каждый шаг
- 🐛 Находить и исправлять баги
- 🎨 Дорабатывать дизайн
- 🧪 Тестировать в браузере

---

## ⏱️ РЕАЛЬНЫЙ TIMELINE

**С Cursor AI + твоя проверка:**

- День 1: API + Types + 2 компонента = **4 часа**
- День 2: Остальные компоненты + интеграция = **4 часа**  
- День 3: Dark mode = **3 часа**
- День 4: Баг фиксы + полировка = **3 часа**
- День 5: Maps (опционально) = **2 часа**

**Итого: 2-3 дня РЕАЛЬНОЙ работы** (с перерывами)

**Без Cursor AI:**
- Всё то же самое = **10-12 дней**

**Вывод:** Cursor ускоряет в 4-5 раз, но НЕ делает 100% автоматически.

---

## 💡 ФИНАЛЬНЫЙ СОВЕТ

**Работай так:**

1. **Утро:** Дай Cursor 2-3 задачи подряд
2. **День:** Проверь, протестируй, исправь баги
3. **Вечер:** Ещё 2-3 задачи
4. **На следующий день:** Продолжай

**НЕ жди что Cursor сделает всю неделю за 1 час!**

Cursor - это **ускоритель**, а не **замена** программиста.

Но с ним ты сделаешь за 2-3 дня то, на что ушла бы неделя.

Удачи! 🚀
