## AI Triathlon Coach — обзор проекта

AI Triathlon Coach — это личный AI‑тренер для триатлета, который:
- хранит профиль атлета и цели в базе,
- подтягивает историю тренировок из Strava,
- считает авто‑метрики (стрик, средние часы, разбивку по видам спорта),
- рассчитывает тренировочные зоны,
- генерирует недельные и многонедельные планы с помощью GPT,
- отправляет отчёты и планы на почту,
- предоставляет современный веб‑интерфейс (Next.js).

Проект состоит из двух основных частей:
- **Бекэнд** (`FastAPI`, `SQLAlchemy`, SQLite, интеграция со Strava и OpenAI) — директория корня.
- **Фронтенд** (`Next.js 14 App Router`, `React`, `TypeScript`) — директория `frontend/`.

### Технологии

- **Backend**: FastAPI, SQLAlchemy ORM, Alembic, Pydantic, JWT‑аутентификация, HTTPX (Strava), Tenacity (retry), собственные модули анализа/планирования.
- **Frontend**: Next.js (app router), React, TypeScript, Tailwind‑подобная утилитарная верстка (классы `bg-slate-...`), Axios.
- **Интеграции**: Strava OAuth2, OpenAI Chat Completions.

---

## Архитектура и доменные сущности

- `models.py` — SQLAlchemy‑модели:
  - `User` — учётка пользователя + Strava токены.
  - `AthleteProfileDB` — профиль атлета (возраст, пол, опыт, auto‑метрики, зоны, предпочтения и т.д.).
  - `GoalDB` — цели/гонки (тип, дата, таргет‑время, primary/secondary).
  - `WeeklyPlanDB`, `ActivityDB`, `TrainingLoadDB` — кэш и планы.
- `schemas.py` — Pydantic‑схемы (`UserCreate`, `Token`, `ProfileResponse`, `GoalCreate/GoalResponse` и т.д.).
- `crud.py` — работа с БД (создание пользователя, профиля, целей, планов, кеш активностей).
- `auth.py` — JWT‑аутентификация (`create_access_token`, `get_current_user`, декод токена).
- `api_auth.py` — REST‑эндпоинты аутентификации.
- `api_user.py` — профиль и цели пользователя.
- `api_coach.py` — всё, что связано с коучем: профиль коуча (JSON‑профайл), планы, отчёты, зоны, интеграция с аналитикой.
- `strava_client.py` — работа со Strava API (OAuth токены, загрузка активностей, нормализация данных).
- `athlete_profile.py` — JSON‑профайл в `data/athlete_profile.json` (используется коучем и аналитикой).

На фронтенде основные типы находятся в `frontend/types/index.ts`, а HTTP‑клиент — в `frontend/lib/api.ts`.

---

## Аутентификация и пользователи

### Бекэнд

- `POST /auth/register` (`api_auth.register`)  
  - Принимает `UserCreate` (email, username, password, full_name).  
  - Создаёт `User` и дефолтный `AthleteProfileDB`.  
  - Возвращает `Token` (JWT + `user`).

- `POST /auth/login` (`api_auth.login`)  
  - Принимает OAuth2‑форму (email как `username`, пароль).  
  - Проверяет пароль, возвращает `Token`.

- `GET /auth/me` — возвращает текущего пользователя по JWT.

JWT хранится на фронтенде в `localStorage` (`token` + `user`), добавляется в `Authorization: Bearer ...` через Axios‑интерсептор (`frontend/lib/api.ts`).

### Фронтенд

- `frontend/lib/auth.ts` — функции `isAuthenticated`, `logout`, работа с localStorage.
- Страницы:
  - `/register` — регистрация, вызывает `authAPI.register`.
  - `/login` — логин, вызывает `authAPI.login`, сохраняет `token` и `user`.
  - `/` (`app/page.tsx`) — роутер:
    - если нет токена → редирект на `/login`,
    - если токен есть и есть primary goal → `/dashboard`,
    - если токен есть, но primary goal нет → `/onboarding`.

---

## Профиль атлета и цели

### Профиль (БД)

- `GET /profile` (`api_user.get_profile`)  
  - Возвращает `ProfileResponse` для текущего пользователя.  
  - Если профиля нет — создаёт дефолтный.

- `PATCH /profile` (`api_user.update_profile`)  
  - Принимает частичный `ProfileUpdate` (возраст, пол, primary_discipline, hours/week и т.д.).

Фронтенд:
- Профиль используется на:
  - `/onboarding` — шаг 1 (доступные часы, опыт, основная дисциплина).
  - `/dashboard` — отображение `Avg Hours (12w)`, доступных часов, основной дисциплины.

### Цели (Goals)

- `POST /goals` — создаёт новую цель (`GoalCreate`), при `is_primary=true` сбрасывает другие primary.
- `GET /goals` — список всех целей пользователя (с опцией `include_completed`).
- `GET /goals/primary` — текущая основная цель (404, если не задана).

Фронтенд:
- `/goals` (`app/goals/page.tsx`):
  - Форма добавления/обновления цели:
    - **Sport**: Triathlon / Run / Bike / Swim.
    - **Race distance** — селект в зависимости от спорта (например, `Half Ironman 70.3`, `Full Ironman 140.6`, `5K`, `Marathon`).
    - **Race date** — `type="date"`.
    - **Target time (HH:MM:SS)` — `type="time" step=1`.
    - Флаг `Set as primary race`.
  - Блок `Primary race` + список `All goals`.
- `/onboarding` — аналогичная форма на шаге 2 онбординга (создание первой primary goal).
- `/dashboard`:
  - Карточка `Primary Goal` и отдельный блок `All goals` с расстоянием до старта.

---

## Coach‑функционал и тренировочные планы

Основная логика коуча находится в:
- `coach.py` — модели `GoalInput`, `WeeklyPlanRequest` и функции:
  - `run_initial_assessment` — первоначальная оценка с помощью GPT.
  - `run_weekly_plan` — генерация недельного плана (структура JSON с днями и сессиями).
- `api_coach.py` — HTTP‑эндпоинты для фронтенда.

Ключевые эндпоинты:

- `POST /coach/plan` — генерация недельного плана:
  - Вход: `WeeklyPlanRequest` (цель, start_date, часы/неделю).
  - История тренировок подтягивается через Strava (`fetch_recent_activities_for_coach`).
  - Результат сохраняется в план‑хранилище (`plan_storage.save_weekly_plan`) и/или БД.

- `POST /coach/weekly_plan_email` — сгенерировать недельный план и отправить на email с HTML‑оформлением.

- `POST /coach/multi_week_plan_email` — сгенерировать многонедельный план (12 недель), экспортировать в `.ics` и отправить письмо с обзором фаз и ссылкой на календарь.

- `POST /coach/weekly_report_email` — сгенерировать недельный отчёт:
  - анализ прогресса (`progress.run_progress_tracker`),
  - анализ нагрузки, формы, усталости,
  - прогнозы по цели,
  - сравнение плана и факта,
  - HTML‑отчёт + письмо.

Фронтенд (`/dashboard` → блок **Quick Actions**):
- `Generate this week's plan` → `coachAPI.generateWeeklyPlan`.
- `Export this week's plan to calendar (.ics)` → `coachAPI.exportWeeklyPlanToCalendar`.
- `Send 12-week plan to email` → `coachAPI.sendMultiWeekPlanEmail`.
- `Send weekly report email` → `coachAPI.sendWeeklyReportEmail`.
- Для каждой кнопки есть:
  - собственное состояние загрузки (`currentAction`, `planStatus`),
  - текст «Generating… / Sending…»,
  - текстовый статус под блоком (какая неделя, сколько часов, имя файла, readiness‑score и т.д.).

---

## Тренировочные зоны и Strava‑метрики

### Автоматический расчёт зон

Модули:
- `training_zones.py` — расчёт зон по бегу, вело (по FTP), плаванию (по CSS).
- `strava_client.py` — загрузка активностей со Strava.
- `api_coach._analyze_activities_for_profile` — анализ N недель активности:
  - считает `weeks_analyzed`, `current_streak`, `longest_streak`,
  - средние часы за 12/52 недель,
  - средние часы по дисциплинам.

Эндпоинты:

- `POST /coach/profile/auto_from_history`:
  - Тянет последние недели из Strava (`fetch_activities_last_n_weeks`).
  - Считает метрики `_analyze_activities_for_profile`.
  - Обновляет:
    - JSON‑профайл (`athlete_profile.json`),
    - профиль в БД (`AthleteProfileDB`) для текущего пользователя:
      `auto_weeks_analyzed`, `auto_avg_hours_last_12_weeks` и др.

- `POST /coach/zones/auto_from_activities`:
  - Ищет лучшие усилия (10K, HM, 5K, swim 1500m).
  - Рассчитывает зоны бега/плавания, сохраняет в JSON‑профайл.
  - Возвращает статус, найденные `best_efforts`, флаги `zones_calculated` и обновлённый профиль.

- `POST /coach/zones/manual`:
  - Принимает ручной ввод:
    - тип и время забега (5K/10K/HM/Marathon),
    - FTP для вело,
    - CSS для плавания,
  - считает зоны и сохраняет в JSON‑профайл.

Фронтенд (`/dashboard` → блок **Training Zones**):
- Верхняя часть:
  - статус `Last updated`,
  - бейджи `Run/Bike/Swim zones ready/not set`.
- Кнопка **Auto-calc from Strava history**:
  - во время запроса текст «Calculating zones from Strava…»,
  - при успехе: «Zones were set from Strava history.»,
  - при отсутствии гонок: «No race efforts found in Strava history. Try manual input below.».
- Блок **Manual input**:
  - бег (тип дистанции + время в формате `MM:SS` или `HH:MM:SS`),
  - FTP (в Вт),
  - CSS (в формате `MM:SS` на 100м),
  - при сохранении — «Zones were updated from manual input.» или понятная ошибка формата.

---

## Интеграция со Strava

### Бекэнд

- `GET /` (`main.root`) — страница с ссылкой на Strava OAuth:
  - собирает URL `https://www.strava.com/oauth/authorize` с `client_id`, `redirect_uri`, `scope`.

- `GET /auth/strava/callback`:
  - принимает `code`,
  - вызывает `strava_client.exchange_code_for_token`,
  - сохраняет токены в `strava_token.json` и возвращает JSON `"Strava authorization successful"`.

- `GET /strava/activities` — прокси к Strava API с постраничной загрузкой.

- `GET /strava/status`:
  - читает `strava_token.json` через `load_tokens`,
  - возвращает:
    - `connected: true/false`,
    - `athlete_name`, `athlete_id`, `expires_at` (если есть токены).

### Фронтенд

- `stravaAPI.getStatus()` (`frontend/lib/api.ts`) → `GET /strava/status`.
- `/coach` (`CoachProfilePage`):
  - при загрузке параллельно тянет `coachAPI.getProfile` и `stravaAPI.getStatus`;
  - отображает блок **Strava connection**:
    - `Connect Strava` / `Reconnect Strava` (ссылка на `API_URL/`),
    - текст «Connected as …» или «Not connected to Strava yet.»;
  - поясняет, что Strava используется для авто‑метрик и зон.

---

## Запуск проекта локально

### 1. Бекэнд

```bash
cd E:\ai_coach_backend
python -m venv venv
.\venv\Scripts\Activate.ps1
pip install -r requirements.txt

uvicorn main:app --reload --port 8000
```

Нужные переменные окружения (через `.env` или системно):
- `SECRET_KEY` — для JWT.
- `OPENAI_API_KEY` — ключ OpenAI.
- `STRAVA_CLIENT_ID`, `STRAVA_CLIENT_SECRET`, `STRAVA_REDIRECT_URI` — для Strava OAuth.

Swagger бекэнда: `http://localhost:8000/docs`.

### 2. Фронтенд

```bash
cd E:\ai_coach_backend\frontend
npm install
npm run dev
```

Фронтенд: `http://localhost:3000`.

---

## 📚 Дополнительная документация

Я провёл детальный анализ вашего проекта с разных точек зрения. Результаты в 5 документах:

### 1. 📋 [Executive Summary](./executive_summary.md)
**Краткая версия для быстрого ознакомления (5 минут чтения)**

Содержит:
- Текущее состояние проекта (что работает, что нет)
- Оценка по критериям (техника, промпт, полезность)
- Топ-5 приоритетных улучшений
- Топ-5 новых фич
- Market opportunity
- Timeline и финансовая модель
- Рекомендации и next steps

👉 **Начните с этого документа!**

---

### 2. 🔍 [Детальный анализ проекта](./project_analysis.md)
**Полный технический и бизнес-анализ (30-40 минут чтения)**

Содержит:

#### 1. Техническая реализация
- ✅ Сильные стороны архитектуры
- ⚠️ Критические проблемы и решения
- 🔧 Предложения по рефакторингу
- Примеры кода для БД (SQLAlchemy models)

#### 2. Анализ промпта
- ✅ Что сделано хорошо
- ⚠️ Проблемы промпта
- 🔧 Полностью переписанный промпт с:
  - Training zones (HR, pace, power)
  - Примерами идеальных планов
  - Race-specific targets
  - Brick workouts
  - Nutrition strategy

#### 3. Полезность результатов
- Что работает
- Что не хватает
- Визуализация, адаптация, nutrition

#### 4. Куда двигаться дальше
- Фазы развития (1-4)
- Advanced features
- Pro features
- Platform features

#### 5. Технические рекомендации
- Немедленные действия
- В течение месяца
- CI/CD, логирование

#### 6. Монетизация
- Freemium модель
- Pricing tiers
- Дополнительные источники дохода

#### 7. Конкурентный анализ
- TrainingPeaks, Humango, Coach by Strava
- Наши преимущества

---

### 3. ✅ [Action Plan](./action_plan.md)
**Практический план с примерами кода (20-30 минут чтения)**

Содержит:

#### Quick Wins (сегодня, 2-4 часа)
1. Исправление модели GPT
2. Добавление .gitignore
3. Вынос констант в config
4. Создание utils.py
5. Базовый error handling

#### Критические улучшения (1-2 недели)
6. Миграция на PostgreSQL (с полным кодом)
7. JWT Authentication (полная реализация)
8. Улучшение промпта (новый TRAINER_SYSTEM_PROMPT)

#### Дополнительные улучшения
9. Pydantic модели для Activities
10. Добавление тестов (pytest examples)

**Каждая секция содержит:**
- Полный код implementation
- Команды для установки
- Примеры использования
- Комментарии

👉 **Используйте как пошаговую инструкцию для реализации!**

---

### 4. 💡 [Идеи новых фич](./new_features.md)
**5 killer features с полной реализацией (30 минут чтения)**

#### 1. Автоматический расчёт зон тренировки
- Концепция
- Полная реализация на Python
- API endpoints
- Примеры usage

#### 2. Race Day Strategy Generator
- Концепция
- Детальный промпт для GPT
- JSON structure для стратегии
- Pacing, nutrition, transitions, mental cues

#### 3. Smart Fatigue Detection
- HR drift анализ
- Pace decline detection
- Missed workouts score
- Автоматические рекомендации

#### 4. Workout Library с фильтрацией
- База готовых тренировок
- Теги и фильтрация
- API endpoints
- Добавление в план одним кликом

#### 5. Визуализация прогресса
- Графики (weekly volume, PMC chart)
- API endpoints для данных
- Recharts integration examples

**Каждая фича содержит:**
- Полную реализацию на Python
- Примеры API endpoints
- Подробные комментарии
- Оценку impact

---

### 5. 🗺️ [Roadmap](./roadmap.md)
**18-месячный план развития (40-50 минут чтения)**

Содержит:

#### Фаза 0: MVP Fix (1 неделя)
- Критические баги
- Estimation: 2 days
- Checklist задач

#### Фаза 1: Multi-user MVP (2-3 недели)
- Infrastructure
- Features
- Launch checklist
- Estimation: 2-3 weeks

#### Фаза 2: Basic Product (4-6 недель)
- Frontend (Next.js)
- Backend enhancements
- Marketing & Growth
- Success metrics

#### Фаза 3: Advanced Features (2-3 месяца)
- Priority 1 features (fatigue, race strategy)
- Priority 2 features (analytics, social)
- Infrastructure (Redis, monitoring)

#### Фаза 4: Scale & Differentiation (3-6 месяцев)
- Unique features (video analysis, voice coach)
- Community & Social
- Business features (marketplace, B2B)

#### Фаза 5: Enterprise & Global (6-12 месяцев)
- Enterprise features
- Global expansion
- Research & Development

**Также включает:**
- Оценка ресурсов для каждой фазы
- Бюджеты и team size
- Риски и митигация
- Метрики для tracking
- Go-to-market strategy
- Финансовая модель (revenue projections)
- Timeline визуализация

---

## 🎯 Как использовать эту документацию

### Если у вас 5 минут:
→ Читайте **Executive Summary**

### Если хотите начать кодить прямо сейчас:
→ Читайте **Action Plan** → копируйте код → внедряйте

### Если нужны идеи новых фич:
→ Читайте **New Features** → выбирайте что реализовать

### Если планируете long-term стратегию:
→ Читайте **Roadmap** → адаптируйте под свои ресурсы

### Если нужен полный контекст:
→ Читайте **Detailed Analysis** → понимайте всю картину

---

## 📊 Краткие выводы

### ✅ Сильные стороны проекта
1. Правильная архитектура (модульная, асинхронная)
2. Strava + GPT интеграция работает
3. Plan vs Fact — killer feature
4. Реальная проблема с большим рынком

### ❌ Критические проблемы
1. Модель GPT не существует (gpt-5.1 → gpt-4o)
2. Нет multi-user support
3. JSON вместо БД
4. Промпт слишком общий
5. Отсутствие error handling

### 🚀 Топ-5 приоритетов
1. Исправить критические баги (2 дня)
2. Multi-user + Auth (1 неделя)
3. Улучшить промпт (2 дня)
4. Web UI (1-2 недели)
5. Визуализация (1 неделя)

### 💰 Потенциал
- Market: 1-3M триатлетов в мире
- Target: 50-100K активных пользователей
- Revenue: $10K-30K MRR через 12-18 месяцев
- Funding: Bootstrap → Seed ($500K-1M) → Series A ($3-5M)

---

## 🛠️ Quick Start Commands

### Исправить критические баги (сегодня)
```bash
# Исправить модель GPT
sed -i 's/gpt-5.1/gpt-4o/g' coach.py progress.py

# Добавить .gitignore
echo "strava_token.json
*.pyc
__pycache__/
.env" > .gitignore

# Установить зависимости для retry logic
pip install tenacity structlog
```

### Начать миграцию на PostgreSQL (эта неделя)
```bash
# Установить зависимости
pip install sqlalchemy psycopg2-binary alembic

# Создать models.py, database.py (см. Action Plan)
# Запустить миграцию
python migrate_to_db.py
```

### Добавить JWT auth (эта неделя)
```bash
# Установить зависимости
pip install python-jose[cryptography] passlib[bcrypt]

# Создать auth.py (см. Action Plan)
# Обновить endpoints в main.py
```

---

## 📞 Контакт и вопросы

Если у вас есть вопросы по документации или нужна помощь с реализацией:
1. Перечитайте соответствующий раздел в документах
2. Проверьте примеры кода в Action Plan
3. Следуйте timeline в Roadmap

---

## ✨ Заключение

**Проект имеет огромный потенциал!** 🚀

Все необходимые технические и бизнес-рекомендации предоставлены. Следующий шаг — начать implementation по приоритетам.

**Success = Technical Excellence + Great UX + Strong Marketing**

Удачи с запуском! 💪

---

*Документация создана на основе анализа 15 файлов проекта*
*Последнее обновление: 2025-11-25*
